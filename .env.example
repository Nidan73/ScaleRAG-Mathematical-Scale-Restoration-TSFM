# Copy to `.env` and fill in locally. NEVER commit the real .env (gitignored).
# These are read from the environment; secrets never live in tracked files.

# GitHub MCP server (fine-grained PAT, read-only scopes recommended):
GITHUB_MCP_PAT=

# Dataset access (only when you reach the data phase — not this phase):
# KAGGLE_USERNAME=
# KAGGLE_KEY=
# HUGGINGFACE_HUB_TOKEN=
